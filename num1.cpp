#include <iostream>
# include "middle_str.h"
#include <string>
using namespace std;
bool itc_isDigit(unsigned char c){
 if (c >= '0' && c <= '9') {
        return true;
    } else {
        return false;
    }
}
